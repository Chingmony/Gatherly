# Normalize line endings. Keep shell scripts LF so the Gradle wrapper runs on Linux/CI.
* text=auto eol=lf

*.bat text eol=crlf
gradlew text eol=lf
*.sh text eol=lf

# Binary assets — never normalize.
*.jar binary
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
