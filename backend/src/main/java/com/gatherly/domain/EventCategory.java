package com.gatherly.domain;

/** Event category — drives the card banner colour and discovery filtering. */
public enum EventCategory {
  CONFERENCE,
  FESTIVAL,
  NETWORKING,
  WORKSHOP,
  GALA,
  HACKATHON,
  OTHER
}
