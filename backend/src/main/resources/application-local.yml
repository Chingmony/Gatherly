# Local development profile — backing services from docker-compose.yml (docs/12 §3).
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.gatherly: DEBUG
    org.flywaydb: INFO
