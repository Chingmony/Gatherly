plugins {
    // Lets the Java toolchain auto-provision a JDK 21 when the build runs on a different JDK.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.9.0'
}

rootProject.name = 'gatherly'
